#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, nv, ne, v1, v2, l, flag = 0;
    list<int> grafo[30];

    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d%d", &nv, &ne);

        for (int j = 0; j < ne; j++) {
            scanf("%d%d", &v1, &v2);


        }
    }
    return 0;
}
